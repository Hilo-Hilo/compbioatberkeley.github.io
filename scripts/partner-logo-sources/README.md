# Partner logo build sources

`ucsf-logo-navy-rgb.eps` is the official RGB master from the UCSF logo
download package linked in `src/data/partners.ts`. It is retained here so
`npm run assets:partners` can reproducibly render the transparent production
PNG instead of enlarging UCSF's smaller web preview.
